<view class="container">
    <view s-for="items" class="category" data-item="{{item}}" bind:tap="onTagClicked" bind:longpress="longpressed">
        {{item.name}}
        <image class="arrow" src="../../images/right_arrow.png"></image>
    </view>
</view>